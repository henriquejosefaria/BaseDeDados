CREATE PROCEDURE `Add Equipamento` (in nome VARCHAR(45),in descricao VARCHAR(75) )
BEGIN

-- DECLARE x INT;

-- x = LAST_INSERT_ID();

INSERT INT GinasioMiguel(Nome,Descricao)
  VALUE(nome,descricao)
END
